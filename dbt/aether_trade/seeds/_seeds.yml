version: 2

seeds:
  - name: order_status
    description: Static lookup for order status codes — mirrors MART.DIM_ORDER_STATUS.
    columns:
      - name: status_code
        data_tests: [not_null, unique]
